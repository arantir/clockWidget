<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Цвет фона -->
    <corners android:radius="8dp"/> <!-- Закругленные углы -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray"/> <!-- Цвет рамки -->
</shape>